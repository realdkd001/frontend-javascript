# Step 1: Install Node.js version 20.16 using NVM
$ curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash

# Step 2: Exit and reopen terminal to reload nvm

# Step 3: Verify installation of nvm
$ command -v nvm

# Step 4: Install Node.js version 20.16
$ nvm install 20.16

# Step 5: Verify Node.js and npm installation
$ node -v
$ npm -v

# Step 6: Install Sass (version 3.7.4)
$ npm install sass@3.7.4

# Step 7: Verify Sass installation
$ npx sass --version
